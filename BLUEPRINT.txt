swarmbotics-connectivity-layer/
├── routing-manager/                    # Triple-Redundant Comms: Dynamic Routing
│   ├── src/
│   │   ├── link-quality-monitor.cpp    # Continuously pings and measures latency/loss on all links
│   │   └── traffic-shaper.cpp          # Prioritizes C2 telemetry over video during low bandwidth
│   ├── scripts/
│   │   └── failover-daemon.py          # Logic to seamlessly switch between MANET, 5G, and SATCOM
│   └── config/
│       └── routing-policy.yaml         # Defines triggers for switching networks
│
├── radio-interfaces/                   # Hardware-specific network drivers
│   ├── manet-mesh/                     # Localized peer-to-peer networking
│   │   ├── tactical-radio-driver.py    # Integrates with mesh radios (e.g., Silvus, TrellisWare)
│   │   └── mesh-topology-tracker.cpp   # Maps the current local swarm mesh network
│   ├── cellular-5g/
│   │   └── modem-controller.py         # Handles AT commands, SIM states, and 5G connections
│   └── leo-satcom/
│       └── satcom-api-client.py        # Polls Starlink/LEO terminal for connection health
│
├── streaming-pipeline/                 # Video & Telemetry Streaming
│   ├── webrtc-stack/
│   │   ├── signaling-server.js         # Establishes WebRTC connections between robots and operators
│   │   └── ice-config.json             # STUN/TURN server configurations
│   ├── media-encoding/
│   │   ├── gstreamer-h265-hw.sh        # Hardware-accelerated low-latency video encoding
│   │   └── udp-telemetry-mux.cpp       # Packs ROS 2 JSON telemetry into raw UDP streams
│   └── qos-manager/
│       └── adaptive-bitrate.py         # Dynamically lowers video resolution if comms degrade
│
└── security-layer/                     # Network Security & Encrypted Tunnels
    ├── vpn-tunnels/
    │   ├── wireguard-setup.sh          # Configures ultra-fast, modern E2E encrypted tunnels
    │   └── key-rotation-daemon.py      # Automatically rotates cryptographic keys in the field
    ├── intrusion-detection/
    │   └── ids-rules.conf              # Monitors for jamming, spoofing, or unauthorized access
    └── nat-traversal/
        └── custom-hole-punching.cpp    # Bypasses complex firewalls to maintain P2P swarm links



